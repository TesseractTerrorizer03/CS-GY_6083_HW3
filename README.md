# CS-GY_6083_HW3
Q1 app code

Install the required libraries in a virtual environment using:

``pip install flask psycopg2-binary``

You will also have to change the database credentials recorded in the variable ``DB_CONFIG`` in order to run locally.
